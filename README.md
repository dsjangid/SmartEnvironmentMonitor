# SmartEnvironmentMonitor
ESP32-based IoT project that reads temperature and humidity from a DHT22 sensor, publishes data over MQTT, stores readings with Python and SQLite, and displays live telemetry on a Flask-powered dashboard.
